#include<stdio.h>
void sortarray(int arr[],int size)
{
    int count 0=0,count1=0,count2=0;
    for(int i=0;i<size;i++)
    {
        if(arr[i]==0)count 0++;
        else if(arr[i]==1)count1++;
        else if(arr[i]==2)count2++;
    }
    for(int i=0;i<count0;i++)
    arr[i]=0;
    for(int i=count0;i<count0;i<count0+count1;i++)arr[i]=1;
    for(int i=count0+count1;i<sizei++)arr[i]=2;
}
int main()
{
    int size;
    printf("enter array size:");
    scanf("%d",&size);
    int arr[size];
    printf("enter %d element (0s,1s,2s only):",size);
    for(int i=0;i<size;i++)
    {
        scanf("%d",&arr[i]);
    }
    sort Array,size;
    printf("Sorted array:");
    for(int i=0;i<size;i++)
    {
        printf("%d",arr[i]);
    }
    printf("\n");
    return 0;
}
